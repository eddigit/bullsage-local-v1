# Trading routes - placeholder that imports from main server
from fastapi import APIRouter

router = APIRouter(prefix="/trading", tags=["Trading Analysis"])

# Note: Trading routes are still in the main server.py
# This placeholder allows the import system to work
