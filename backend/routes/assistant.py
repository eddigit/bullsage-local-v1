# Assistant routes - placeholder that imports from main server
from fastapi import APIRouter

router = APIRouter(prefix="/assistant", tags=["AI Assistant"])

# Note: Assistant routes are still in the main server.py
# This placeholder allows the import system to work
