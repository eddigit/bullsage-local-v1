# Academy routes - placeholder that imports from main server
from fastapi import APIRouter

router = APIRouter(prefix="/academy", tags=["Academy"])

# Note: Academy routes are still in the main server.py
# This placeholder allows the import system to work
